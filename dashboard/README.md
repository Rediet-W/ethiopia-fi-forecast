# Streamlit Dashboard - Selam Analytics

## Running Locally

1. Navigate to the dashboard directory:
   ```bash
   cd dashboard
   ```

Install dependencies:

```Bash
pip install streamlit pandas plotly
```

Run the Streamlit application:

```Bash
streamlit run app.py
```
